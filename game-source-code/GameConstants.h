#ifndef GAME_CONSTANTS_H
#define GAME_CONSTANTS_H

const auto WINDOW_WIDTH = 800;
const auto WINDOW_HEIGHT = 600;
const auto PLAYER_SPEED = 5.0f;
const auto LASER_SPEED = 70.0f;
const auto LANDER_SPEED = 2.0f;
const auto SPAWN_INTERVAL = 3000;          // Spawn a new Lander every 2 seconds
const auto HUMANOID_SPAWN_INTERVAL = 2000; // Spawn a new Humanoid every 10 seconds
const auto MISSILE_INTERVAL = 3000;        // Fire a missile every 2 seconds
const auto FUEL_SPAWN_INTERVAL = 5000;     // Adjust the time interval as needed

#endif // GAME_CONSTANTS_H
