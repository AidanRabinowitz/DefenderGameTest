#ifndef GAME_CONSTANTS_H
#define GAME_CONSTANTS_H

const auto WINDOW_WIDTH = 800;
const auto WINDOW_HEIGHT = 600;
const auto PLAYER_SPEED = 8.0f;
const auto LASER_SPEED = 50.0f;
const auto LANDER_SPEED = 1.0f;
const auto SPAWN_INTERVAL = 3000;      // Spawn a new Lander every 3 seconds
const auto MISSILE_INTERVAL = 4000;    // Fire a missile every 4 seconds
const auto FUEL_SPAWN_INTERVAL = 5000; // Fuels spawn every 5 seconds

#endif // GAME_CONSTANTS_H
